{
  "name": "build-rapier-module",
  "description": "Build scripts for which produces a es6 module which will work in a buildless environment",
  "private": true,
  "scripts": {
    "build-rust-2d": "wasm-pack build ../rapier2d --target web --out-dir ../rapier-module/build/pkg2d",
    "build-rust-3d": "wasm-pack build ../rapier3d --target web",
    "build-rust": "npm run build-rust-2d && npm run build-rust-3d",
    "build": "npm run clean && npm run build-rust && npx webpack",
    "webpack": "npx webpack",
    "clean": "npx rimraf ./dist ./build",
    "all": "npm run build"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^7.0.0",
    "create-file-webpack": "^1.0.2",
    "rimraf": "^3.0.2",
    "ts-loader": "^8.0.12",
    "typescript": "^4.1.3",
    "webpack": "^5.11.0",
    "webpack-cli": "^4.3.0"
  }
}
